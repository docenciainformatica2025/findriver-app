import React from 'react';
import { Typography, Box } from '@mui/material';

const Reports = () => {
    return (
        <Box>
            <Typography variant="h4">Reportes</Typography>
            <Typography>Página de reportes en construcción.</Typography>
        </Box>
    );
};

export default Reports;
