import React from 'react';
import { Typography, Box } from '@mui/material';

const Transactions = () => {
    return (
        <Box>
            <Typography variant="h4">Transacciones</Typography>
            <Typography>Página de transacciones en construcción.</Typography>
        </Box>
    );
};

export default Transactions;
